# Secrets — never commit
.env
.env.*
!.env.example
*.pem
*.key

# Data pulled from production — contains real staff names and emails
docs/teams-raw.json
docs/departments-raw.json
docs/teams-*.raw.json
frontend/src/mocks/teams-real.json

# Dependencies / build output
node_modules/
dist/
build/
target/
.vite/

# Editor / OS
.DS_Store
Thumbs.db

# Pseudonymized build artifacts (regenerated by npm run pseudonymize)
standalone/src/mocks/*.public.json
standalone/dist/

# Production captures — real staff names, emails and a live organization API key.
# These are working inputs, not deliverables; keep them local.
docs/Walkthough.md
docs/prod-run-fixtures.json
docs/prod-teams-v2.json
docs/n8n-fixture-payload.json
docs/payers-raw.json
docs/payers-reference.json
docs/v2-tool-calls.json
docs/v2-tool-calls.md
docs/departments-observed.csv
docs/workitem_profile.xlsx
docs/Employee Name and Designation (Onsite and Offshore).xlsx
docs/*.raw.json

# n8n exports carry a hardcoded X-API-Key in the trigger.
docs/RCM Auto Assign - Midnight Trigger.json

# Local scratch / build artifacts
q.json
standalone/.schema-bundle.mjs

# Real staff identities — never commit. The committed *.json are pseudonymized;
# these hold the originals for local work.
standalone/src/mocks/*.local.json

# The v1 workflow export embeds a live organization API key.
docs/RCM Auto-Assignment (1).json
